## 1. Adaptive Digest Guidance

- [x] 1.1 Update `literature-analysis/SKILL.md` and `references/digest_generation.md` so adaptive depth is explicit and detailed numeric guidance has one source of truth.
- [x] 1.2 Consolidate `literature-digest/SKILL.md` around one language-neutral adaptive-depth rule with widened soft ranges and anti-padding guidance.

## 2. OpenSpec Capability Alignment

- [x] 2.1 Update the existing `literature-digest` main-spec Purpose to describe the current digest-only skill and verify the realignment/retirement deltas cover every displaced requirement.

## 3. Validation

- [x] 3.1 Run strict OpenSpec validation, relevant existing guidance checks, diff hygiene checks, and manual normal/long/sparse source acceptance review without adding instruction-text snapshot tests.
