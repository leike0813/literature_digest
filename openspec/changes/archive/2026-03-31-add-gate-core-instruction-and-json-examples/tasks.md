# Tasks

- [x] Add an active change for gate core instruction replay and final JSON examples
- [x] Add `assets/core_instruction.md` and return it as `core_instruction` from gate payloads
- [x] Restructure `SKILL.md` to include a compact core instruction section without removing the existing main-path guidance
- [x] Update runner prompts and gate interface docs to explain `core_instruction` vs `execution_note`
- [x] Add concrete final stdout JSON examples to the relevant guidance docs
- [x] Update tests to cover `core_instruction` and JSON-example guidance expectations
