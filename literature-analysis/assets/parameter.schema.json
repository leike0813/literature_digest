{
    "type": "object",
    "properties": {
        "language": {
            "type": "string",
            "default": "zh-CN",
            "description": "Target language for the digest"
        },
        "score_only": {
            "type": "boolean",
            "default": false,
            "description": "When true, run source normalization and paper scoring only; skip digest, references, and citation analysis"
        }
    },
    "required": []
}
